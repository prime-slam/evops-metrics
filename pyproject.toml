[tool.poetry]
name = "evops"
version = "0.1.0"
description = "Evaluation of Plane Segmentation."
authors = ["Dmitry Yarosh", "Pavel Mokeev"]
license = "APACHE"

[tool.poetry.dependencies]
python = "^3.9"
numpy = "^1.21.5"
nptyping = "^1.4.4"
open3d = "^0.14.1"

[tool.poetry.dev-dependencies]
black = "^22.1.0"
pre-commit = "^2.17.0"
pytest = "^7.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
